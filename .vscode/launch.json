{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "docker compose up --build app",
      "name": "Docker Compose: Dev",
      "request": "launch",
      "type": "node-terminal"
    },
    {
      "command": "docker compose up --build portfolio",
      "name": "Docker Compose: Preview",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
